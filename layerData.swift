


import Foundation
import UIKit

struct layerData {
    static var player = [Player(name:"Smagul",image: UIImage.init(named: "test3.jpg")),
                         Player(name:"brook",image: UIImage.init(named: "test3.jpg"))]
    func corted() {
        for_
    }
}
